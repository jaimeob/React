/*
 * IndicadoresDireccion Messages
 *
 * This contains all the text for the IndicadoresDireccion container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IndicadoresDireccion';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IndicadoresDireccion container!',
  },
});
