// import { fromJS } from 'immutable';
// import { selectIndicadoresDireccionDomain } from '../selectors';

describe('selectIndicadoresDireccionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
