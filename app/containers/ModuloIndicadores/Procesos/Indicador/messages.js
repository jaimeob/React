/*
 * Indicador Messages
 *
 * This contains all the text for the Indicador container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Indicador';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Indicador container!',
  },
});
