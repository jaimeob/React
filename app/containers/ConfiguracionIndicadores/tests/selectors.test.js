// import { fromJS } from 'immutable';
// import { selectConfiguracionIndicadoresDomain } from '../selectors';

describe('selectConfiguracionIndicadoresDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
