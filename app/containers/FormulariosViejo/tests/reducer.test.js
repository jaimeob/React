// import { fromJS } from 'immutable';
// import formulariosReducer from '../reducer';

// describe('formulariosReducer', () => {
//   it('returns the initial state', () => {
//     expect(formulariosReducer(undefined, {})).toEqual(fromJS({}));
//   });
// });
