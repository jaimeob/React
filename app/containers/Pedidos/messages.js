/*
 * Pedidos Messages
 *
 * This contains all the text for the Pedidos container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Pedidos';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Pedidos container!',
  },
});
