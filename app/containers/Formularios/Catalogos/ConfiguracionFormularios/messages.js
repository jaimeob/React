/*
 * Formulario Messages
 *
 * This contains all the text for the Formulario container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Formulario';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Formulario container!',
  },
});
