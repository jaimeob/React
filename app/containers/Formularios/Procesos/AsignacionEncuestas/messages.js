/*
 * Asignacion Messages
 *
 * This contains all the text for the Asignacion container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Asignacion';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Asignacion container!',
  },
});
