// import { fromJS } from 'immutable';
// import { selectFormulariosUsuariosDomain } from '../selectors';

describe('selectFormulariosUsuariosDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
