/*
 * MonitorVale Messages
 *
 * This contains all the text for the MonitorVale container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MonitorVale';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MonitorVale container!',
  },
});
