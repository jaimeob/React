// import { fromJS } from 'immutable';
// import { selectConfiguracionTicketsDomain } from '../selectors';

describe('selectConfiguracionTicketsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
