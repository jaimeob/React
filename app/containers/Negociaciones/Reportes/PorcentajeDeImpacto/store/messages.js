/*
 * PorcentajeDeImpacto Messages
 *
 * This contains all the text for the PorcentajeDeImpacto container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PorcentajeDeImpacto';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PorcentajeDeImpacto container!',
  },
});
