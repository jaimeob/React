// import { fromJS } from 'immutable';
// import { selectPorcentajeDeImpactoDomain } from '../selectors';

describe('selectPorcentajeDeImpactoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
