// import { fromJS } from 'immutable';
// import { selectPorcentajeAhorroComprasDomain } from '../selectors';

describe('selectPorcentajeAhorroComprasDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
