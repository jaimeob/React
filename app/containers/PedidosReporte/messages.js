/*
 * PedidosReporte Messages
 *
 * This contains all the text for the PedidosReporte container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PedidosReporte';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PedidosReporte container!',
  },
});
