/*
 * BandejaDifusiones Messages
 *
 * This contains all the text for the BandejaDifusiones container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BandejaDifusiones';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BandejaDifusiones container!',
  },
});
