/*
 * UsuariosAlmacenes Messages
 *
 * This contains all the text for the UsuariosAlmacenes container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UsuariosAlmacenes';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UsuariosAlmacenes container!',
  },
});
