// import { fromJS } from 'immutable';
// import { selectConfiguracionPeriodosDomain } from '../selectors';

describe('selectConfiguracionPeriodosDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
