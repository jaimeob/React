/*
 * InventarioCiclico Messages
 *
 * This contains all the text for the InventarioCiclico container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.InventarioCiclico';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InventarioCiclico container!',
  },
});
