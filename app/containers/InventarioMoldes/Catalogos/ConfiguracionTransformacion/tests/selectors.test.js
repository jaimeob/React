// import { fromJS } from 'immutable';
// import { selectConfiguracionTransformacionDomain } from '../selectors';

describe('selectConfiguracionTransformacionDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
