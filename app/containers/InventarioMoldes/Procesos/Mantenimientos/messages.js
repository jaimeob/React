/*
 * Mantenimientos Messages
 *
 * This contains all the text for the Mantenimientos container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Mantenimientos';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Mantenimientos container!',
  },
});
