/*
 * Inventario Messages
 *
 * This contains all the text for the Inventario container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Inventario';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Inventario container!',
  },
});
