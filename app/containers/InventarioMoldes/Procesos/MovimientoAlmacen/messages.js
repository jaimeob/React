/*
 * MovimientoAlmacen Messages
 *
 * This contains all the text for the MovimientoAlmacen container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MovimientoAlmacen';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MovimientoAlmacen container!',
  },
});
