/*
 * Reportes Messages
 *
 * This contains all the text for the Reportes container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Reportes';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Reportes container!',
  },
});
