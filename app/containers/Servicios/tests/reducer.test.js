// import { fromJS } from 'immutable';
// import serviciosReducer from '../reducer';// eslint-disable-line

// describe('serviciosReducer', () => {
//   it('returns the initial state', () => {
//     expect(serviciosReducer(undefined, {})).toEqual(fromJS({}));
//   });
// });
