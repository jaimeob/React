// import { fromJS } from 'immutable';
// import { selectCatalogoIndicadoresDomain } from '../selectors';

describe('selectCatalogoIndicadoresDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
