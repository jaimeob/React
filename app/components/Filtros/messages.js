/*
 * Filtros Messages
 *
 * This contains all the text for the Filtros component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Filtros';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Filtros component!',
  },
});
