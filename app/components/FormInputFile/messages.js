/*
 * FormInputFile Messages
 *
 * This contains all the text for the FormInputFile component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FormInputFile';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FormInputFile component!',
  },
});
