/*
 * Switch Messages
 *
 * This contains all the text for the Switch component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Switch';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Switch component!',
  },
});
