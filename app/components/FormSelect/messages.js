/*
 * FormSelect Messages
 *
 * This contains all the text for the FormSelect component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FormSelect';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FormSelect component!',
  },
});
