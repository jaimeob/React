/*
 * TablaMaterial Messages
 *
 * This contains all the text for the TablaMaterial component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TablaMaterial';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TablaMaterial component!',
  },
});
