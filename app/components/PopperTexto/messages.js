/*
 * PopperTexto Messages
 *
 * This contains all the text for the PopperTexto component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PopperTexto';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PopperTexto component!',
  },
});
