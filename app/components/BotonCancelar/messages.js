/*
 * BotonCancelar Messages
 *
 * This contains all the text for the BotonCancelar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BotonCancelar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BotonCancelar component!',
  },
});
