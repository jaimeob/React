/*
 * FormRadio Messages
 *
 * This contains all the text for the FormRadio component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FormRadio';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FormRadio component!',
  },
});
