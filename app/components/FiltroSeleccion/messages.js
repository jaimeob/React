/*
 * FiltroSeleccion Messages
 *
 * This contains all the text for the FiltroSeleccion component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.FiltroSeleccion';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FiltroSeleccion component!',
  },
});
