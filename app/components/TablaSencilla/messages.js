/*
 * TablaSencilla Messages
 *
 * This contains all the text for the TablaSencilla component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TablaSencilla';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TablaSencilla component!',
  },
});
