/*
 * BotonSuccess Messages
 *
 * This contains all the text for the BotonSuccess component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BotonSuccess';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BotonSuccess component!',
  },
});
